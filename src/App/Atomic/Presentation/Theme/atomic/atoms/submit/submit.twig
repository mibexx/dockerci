{% setting data = {
    value: 'Button',
    type: 'primary',
    class: ''
} %}
{% block button %}
    <input type="submit" value="{{ data.value }}" class="btn btn-{{ data.type }}{% if data.class != "" %} {{ data.class }}{% endif %}"/>
{% endblock %}