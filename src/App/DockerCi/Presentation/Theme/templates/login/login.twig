{% extends template('layout-center', 'DockerCi') %}

{% block content %}
    <div class="container">
        <div class="row justify-content-md-center">
            <div class="col col-lg-4">
                {% embed organism('login') with {
                    data: {
                        alert: {
                            error: error
                        }
                    }
                } %}{% endembed %}
            </div>
        </div>
    </div>
{% endblock %}